package view.interfaces;

/*Check-In 2. MouseObserver uses run, make an interface for it
Kind of feels redundant as only one class will use it, maybe down the line another
class uses it?*/
public interface IMouseAdapterObserver {
    void run();
}
